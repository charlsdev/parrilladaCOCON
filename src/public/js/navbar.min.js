const showMenu = document.getElementById('nav__toggle'),
   sidebar = document.getElementById('sidebar');

if (sidebar && showMenu) {
   showMenu.addEventListener('click', () => {
      sidebar.classList.toggle('showModal');
      showMenu.classList.toggle('rotate');
   });
}